r1 = Resistor {
    value = 10000;
    };
    
c1 = Capacitor {
    value = 100000;
    };

d1 = Device {
        input = (a, 0);
        output = (b, 0);
        V = (V * 0.3) - 23;
};

// Add them in parallel
//subc = parallel r1 r2
//sub1 = series r1  r2
