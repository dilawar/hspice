r1 = Resistor {
    value = 10000;
    };
    
c1 = Capacitor {
    value = 100000;
    };

d1 = Device {
        in a;
        out b, 0;
        /*V(b) = I(a) + 3.0;*/
};

// Add them in parallel
//subc = parallel r1 r2
//sub1 = series r1  r2
