r1 = Resistor { in a; out b; value = 10000.0; };

c1 = Capacitor {
    value = 100000.0;
    };

d1 = Device {
        in a;
        out b, o;
        /*V(b) = I(a) + 3.0;*/
};

// Add them in parallel
//subc = parallel r1 r2
//sub1 = series r1  r2
